version: '3.8'

services:
  web:
    build: .
    ports:
      - "${PORT:-8000}:8000"
    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
      - DEBUG=False
      - DJANGO_SECRET_KEY=your-production-secret-key-here-change-this
      - PORT=${PORT:-8000}
    command: gunicorn --bind 0.0.0.0:8000 RetainAI.wsgi:application